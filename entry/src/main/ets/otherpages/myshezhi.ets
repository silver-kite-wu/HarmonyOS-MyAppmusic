import { router } from '@kit.ArkUI';
@Entry
@Component
struct Myshezhi {
  @StorageLink('touxiang') touxiang:string=''
  @StorageLink('name')name:string=''
  @StorageLink('sex')sex:string=''
  @StorageLink('age')age:number=NaN
  @StorageLink('year')year:string=''
  @StorageLink('star')star:string=''
  @StorageLink('address')address:string=''
  @StorageLink('num')num:string=''
  build() {
      Column(){
        Row(){
          Image($r('app.media.back'))
            .width(20)
            .fillColor(Color.Black)
            .margin({left:10})
            .onClick(()=>{
              router.back()
            })
          Text('个人资料')
            .margin({left:'28%'})
            .fontSize(24)
            .fontWeight(500)
            .layoutWeight(1)
        }
        .height(50)
        .width('100%')

        List(){
          ListItem(){
            Row(){
              Text('头像')
                .fontSize(20)
                .margin({left:10})
              Row(){
                Image(this.touxiang)
                  .width(30)
                  .height(30)
                  .margin({right:10})
                Image($r('app.media.xiayibu'))
                  .width(24)
                  .onClick(()=>{
                    router.pushUrl({
                      url:'otherpages/touxiang_page'
                    })
                  })
              }
            }
            .border({
              width:{bottom:1},
              color:Color.Gray})
            .justifyContent(FlexAlign.SpaceBetween)
            .width('100%')
            .height(50)
          }

          ListItem(){
            Row(){
              Text('ID')
                .fontSize(20)
                .margin({left:10})
              Row(){
                Text('4562786256')
                  .fontSize(18)
                  .margin({right:30})
              }
            }
            .border({
              width:{bottom:1},
              color:Color.Gray
            })
            .justifyContent(FlexAlign.SpaceBetween)
            .width('100%')
            .height(50)
          }

          ListItem(){
            Row(){
              Text('名字')
                .fontSize(20)
                .margin({left:10})
              Row(){
                Text(this.name)
                  .fontSize(16)
                  .margin({right:5})
                Image($r('app.media.xiayibu'))
                  .width(24)
                  .onClick(()=>{
                    router.pushUrl({
                      url:'otherpages/name_page'
                    })
                  })
              }
            }
            .border({
              width:{bottom:1},
              color:Color.Gray
            })
            .justifyContent(FlexAlign.SpaceBetween)
            .width('100%')
            .height(50)
          }

          ListItem(){
            Row(){
              Text('性别')
                .fontSize(20)
                .margin({left:10})
              Row(){
                Text(this.sex)
                  .fontSize(16)
                  .margin({right:10})
                Image($r('app.media.xiayibu'))
                  .width(24)
                  .onClick(()=>{
                    router.pushUrl({
                      url:'otherpages/sex_page'
                    })
                  })
              }
            }
            .border({
              width:{bottom:1},
              color:Color.Gray
            })
            .justifyContent(FlexAlign.SpaceBetween)
            .width('100%')
            .height(50)
          }

          ListItem(){
            Row(){
              Text('年龄')
                .fontSize(20)
                .margin({left:10})
              Row(){
                Text(`${this.age}岁`)
                  .fontSize(16)
                  .margin({right:30})
              }
            }
            .border({
              width:{bottom:1},
              color:Color.Gray
            })
            .justifyContent(FlexAlign.SpaceBetween)
            .width('100%')
            .height(50)
          }

          ListItem(){
            Row(){
              Text('地区')
                .fontSize(20)
                .margin({left:10})
              Row(){
                Text(this.address)
                  .fontSize(16)
                  .margin({right:10})
                Image($r('app.media.xiayibu'))
                  .width(24)
                  .onClick(()=>{
                    router.pushUrl({
                      url:'otherpages/address_page'
                    })
                  })
              }
            }
            .border({
              width:{bottom:1},
              color:Color.Gray
            })
            .justifyContent(FlexAlign.SpaceBetween)
            .width('100%')
            .height(50)
          }

          ListItem(){
            Row(){
              Text('手机号')
                .fontSize(20)
                .margin({left:10})
              Row(){
                Text(this.num)
                  .fontSize(16)
                  .margin({right:10})
                Image($r('app.media.xiayibu'))
                  .width(24)
                  .onClick(()=>{
                    router.pushUrl({
                      url:'otherpages/num_page'
                    })
                  })
              }
            }
            .border({
              width:{bottom:1},
              color:Color.Gray
            })
            .justifyContent(FlexAlign.SpaceBetween)
            .width('100%')
            .height(50)
          }

          ListItem(){
            Row(){
              Text('生日')
                .fontSize(20)
                .margin({left:10})
              Row(){
                Text(this.year)
                  .fontSize(16)
                  .margin({right:10})
                Image($r('app.media.xiayibu'))
                  .width(24)
                  .onClick(()=>{
                    router.pushUrl({
                      url:'otherpages/birthday_page'
                    })
                  })
              }
            }
            .border({
              width:{bottom:1},
              color:Color.Gray
            })
            .justifyContent(FlexAlign.SpaceBetween)
            .width('100%')
            .height(50)
          }

          ListItem(){
            Row(){
              Text('星座')
                .fontSize(20)
                .margin({left:10})
              Row(){
                Text(this.star)
                  .fontSize(16)
                  .margin({right:30})
              }
            }
            .border({
              width:{bottom:1},
              color:Color.Gray
            })
            .justifyContent(FlexAlign.SpaceBetween)
            .width('100%')
            .height(50)
          }
        }
        .layoutWeight(1)
        .width('100%')
        .scrollBar(BarState.Off)
        .edgeEffect(EdgeEffect.Spring)
      }
      .expandSafeArea([SafeAreaType.SYSTEM],[SafeAreaEdge.TOP,SafeAreaEdge.BOTTOM])
      .width('100%')
      .height('100%')
      .padding({left:10,right:10,top:5,bottom:5})
      .radialGradient({
        center:['90%','-10%'],
        radius:'150%',
        colors:[
          ['#ffab884c',0.5],
          [Color.Transparent,1]
        ]
      })
  }
}